{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${version}",
  "name": "${mod_name}",
  "description": "${mod_description}",
  "authors": [
    "${mod_author}"
  ],
  "contact": {
    "homepage": "${mod_website}",
    "sources": "${mod_repository}"
  },
  "license": "${mod_license}",
  "icon": "icon.png",
  "environment": "*",
  "entrypoints": {
    "main": [
      "com.daqem.jobsplustoolsgenesiscompatibility.fabric.JobsPlusToolsGenesisCompatibilityFabric"
    ]
  },
  "depends": {
    "fabric-api": ">=${fabric_version}",
    "minecraft": ">=${minecraft_version}",
    "jobsplustools": ">=${jobsplustools_version}",
    "genesis": ">=${genesis_version}"
  }
}